检查单臂第一人称视频中的物体 mask。仅根据真实可见轮廓和多帧操作关系判断，候选来源不影响标准。

任务与对象定义：
{task_text}

待检查角色：{role}
操作侧：{active_arm}
操作窗口：{operation_start}–{operation_end}
seed frame：{seed_frame_id}
候选编号：{candidate_ids}

target 按任务定义为被操纵的完整物体、工具或完整独立作用部件。receiver（若要求）是被工具
作用的独立容器或有明确边界的表面，不要求发生放置。不要混淆工具和作用对象。
允许开场已持物、没有闭合事件；通过工作时刻的接触、运动关系确认真正实例。
闲置手里的另一个物体不是 target。相机整体运动不是目标被操纵的证据。

小目标图右侧可能附有同一候选的局部放大图，彩色轮廓才是实际候选，不是其他未圈出的物体。
先在图中确认轮廓的真实实例及其相邻结构，再与动作证据比较；不要把语义假设当作候选标签。

检查候选是否覆盖请求对象全部画内可见部分，是否漏掉相连的可见结构，是否混入人手、夹爪、
背景或另一实例。目标在画外／被遮挡的部分不应补全。把手任务只覆盖完整独立把手，不覆盖
整门；工具任务覆盖整个工具，不只勺头、工具柄或抹布的一角。容器不应只覆盖内部反光／液体。
擦拭 receiver 应对应真实物理边界，不是任意扩大区域或擦拭轨迹。
没有正确候选时 reject_all；身份或作用关系不清楚时 ambiguous。不能为提高通过率降低标准。

只输出 JSON：
{"decision":"accept"|"reject_all"|"ambiguous","selected_candidate":"候选编号"|null,"confidence":0.0,"reason":"不超过20个词的简短理由"}

候选 mask：
{candidate_panels}

操作上下文：
{context_frames}
