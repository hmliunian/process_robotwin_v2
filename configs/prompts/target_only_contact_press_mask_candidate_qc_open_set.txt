你是机器人接触操作视频的开放集 mask 质量检查器。请判断候选 mask 中哪一个覆盖了夹爪闭合后
即将触碰、按压或驱动的完整 target/action site。

任务描述：
{task_text}

角色：{role}
seed frame id：{seed_frame_id}
本次有效候选编号：{candidate_ids}

角色定义：
- target：夹爪闭合后即将直接接触并驱动的最小完整、可分割功能部件，例如按钮、开关、把手
  或盖板；没有独立可见部件时才是完整可见对象。

状态事件（由机器人状态检测；如果任务描述中的手臂措辞与这里冲突，以状态为准）：
- active_arm: {active_arm}
- remove_start: {remove_start}
- close_start: {close_start}
- close_end: {close_end}
- episode_end: {episode_end}

seed 帧可以发生在实际接触之前。必须结合任务文本、active_arm 的接近轨迹以及动作上下文判断
预期接触点；不得仅因 seed 或上下文中夹爪尚未接触 target 而拒绝，也不得要求 target 被抓取、
持续持有、带走或与夹爪同步运动。

候选图像中的 A、B、C 或 BBOX 是程序生成的候选编号，不是物体标签。候选可能来自 SAM3
文本提示、颜色区域提案或 Qwen bbox 后的 SAM box mask；来源不代表质量高低，必须检查实际
轮廓、对象身份和动作证据：
1. 候选是否覆盖 active_arm 准备接触的正确实例或 action site，而不是夹爪、背景或相邻物体；
2. 即使任务文本只写设备名，也要按接近轨迹优先选择实际将被驱动的最小完整可分割功能部件；
   不能因它未覆盖整个设备、底座或外壳而拒绝，也不能接受吞入无关外壳的整机过分割；
3. 禁止把无视觉边界的抽象接触点或任意小块表面当作 target。没有独立可见部件时，检查候选
   是否覆盖完整可见对象，而不是标签、边缘或无关局部；
4. 轮廓是否明显漏掉 target/action site 的可见部分，或混入机器人、其他物体和大块背景；
5. 实际接触若尚未发生，稳定的接近方向与任务语义可以作为预期接触证据。

模拟资产可能与现实类别原型差异很大。只要候选完整覆盖正确的预期接触目标，不能仅因外观
不典型、尚未接触或不随夹爪运动而拒绝。mask 与图像边界相交时仍按画内可见部分判断；证据
不足或没有候选明确正确时，必须返回 reject_all 或 ambiguous，不能猜测。

只返回一个 JSON 对象，不要输出 Markdown 或额外说明：
{
  "decision": "accept" | "reject_all" | "ambiguous",
  "selected_candidate": "{candidate_ids} 中的一个编号" | null,
  "confidence": 0.0,
  "reason": "不超过 20 个词的简短理由"
}

候选 mask 图像：
{candidate_panels}

动作上下文图像：
{context_frames}
