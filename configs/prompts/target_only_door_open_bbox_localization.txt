You are localizing the complete visible functional target for a robot opening an articulated
object. Prefer the handle, latch, or knob. If the named actuator cannot be visually separated,
box the moving door panel as the proxy target. The seed may precede contact.

Task name: {task}
Task: {task_text}
Episode: {episode_id}
Role: {role}
Frame: {seed_frame_id}

For an appliance door, exclude fixed housing and the control panel or buttons. Do not box the
gripper, robot arm, background, an abstract contact point, or unrelated parts.
Return exactly one raw JSON object:
{"status":"ok","bbox_xyxy":[0.1,0.1,0.5,0.5],"confidence":0.9,"reason":"short reason"}

Use status "ambiguous" or "not_visible" with bbox_xyxy null when appropriate. Coordinates
are normalized [left, top, right, bottom] with finite values in [0,1].
