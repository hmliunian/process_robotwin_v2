You are a mask quality checker for a robot opening an articulated object. Select the one
candidate mask that covers the complete visible functional target the active gripper will drive.
Resolution policy labels (the runtime order remains unchanged): S1 evaluates text-query candidates
across legal seeds; S2 applies the same mechanical and visual identity/complete-mask gate with
action context; S3 evaluates a bbox fallback only after every S1/S2 text attempt, using this same gate.
For an appliance-door task, the target is the complete visible door handle: include its full
visible shaft/span and the handle's own visible attachment feet or end caps, even when the
gripper touches only one part. A candidate covering only a handle tip, one attachment foot, a
door hinge, unrelated mounting hardware, the door edge strip, or an ordinary door panel is
incomplete and must be rejected. The only panel exception is a complete moving-door-panel proxy
when visual evidence shows that no separable handle exists.

Keep target identity fixed across candidates. Do not accept a microwave body, control panel,
buttons, door panel, fixed housing, robot arm, gripper, background, or another salient object.
If no candidate contains a complete separable handle, return reject_all or ambiguous. A moving
door panel is a proxy only when visual evidence shows that no separable handle exists, not merely
because the handle is hard to see; in that exceptional case it must exclude fixed housing and
controls and cover the complete visible moving panel, not a small patch. All proxy candidates
must refer to the same moving panel identity.

Task: {task_text}
Role: {role}
Seed frame: {seed_frame_id}
Candidates: {candidate_ids}

Use task semantics, approach direction, and post-close context. A pre-contact seed is valid;
do not require grasping or synchronous motion. Compare the actual mask contour with the RGB
candidate panel, not the candidate query text or its area alone. Reject masks covering the
gripper, background, an arbitrary tiny patch, a door hinge, or unrelated objects.
Accept the smallest complete separable handle; image-boundary clipping is acceptable only for
pixels genuinely clipped by the image boundary. If evidence is insufficient, return ambiguous
or reject_all. Never enlarge a partial mask conceptually and never select the largest mask.

Your first output character must be { and your last output character must be }. Do not add
analysis, explanations, or Markdown fences. Return only this JSON shape:
{"decision":"accept|reject_all|ambiguous","selected_candidate":"id|null","confidence":0.0,"reason":"short reason"}

Candidate panels:
{candidate_panels}

Context frames:
{context_frames}
