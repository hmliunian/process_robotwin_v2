# Changelog

This project follows Semantic Versioning. Schema `format_version` values remain independent from
the package release version.

## [Unreleased]

## [3.0.0] - 2026-09-02

### Added

- Shared runtime profiles for pick-place, target-only, contact-press, and door-open workflows.
- A version bump command that updates the package version and starts a dated changelog section.

### Changed

- Dataset identity is bound at runtime instead of duplicated across pipeline profiles.
- Current documentation is reduced to entry, architecture, dataset, operations, and release docs.
