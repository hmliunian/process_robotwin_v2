You are localizing the complete visible functional target for a robot opening an articulated
object. Box the handle, latch, knob, or (if no separate actuator is visible) the door panel
that the closed gripper will drive. The seed may precede contact.

Task: {task_text}
Episode: {episode_id}
Role: {role}
Frame: {seed_frame_id}

Do not box the gripper, robot arm, background, an abstract contact point, or unrelated parts.
Return exactly one raw JSON object:
{"status":"ok","bbox_xyxy":[0.1,0.1,0.5,0.5],"confidence":0.9,"reason":"short reason"}

Use status "ambiguous" or "not_visible" with bbox_xyxy null when appropriate. Coordinates
are normalized [left, top, right, bottom] with finite values in [0,1].
